# Trinkenbot Enhanced - Telegram Trading Bot

## Огляд

Автоматизований торговий бот для пошуку та виконання арбітражних можливостей між DEX (децентралізованими біржами) та централізованою біржею XT.com.

### Ключові можливості

✅ **Багатоблокчейновий моніторинг**: Ethereum, BSC
✅ **DEXScreener інтеграція**: Автоматичне сканування нових токенів
✅ **XT.com інтеграція**: Перевірка наявності та торгівля
✅ **Автоматична торгівля**: Виконання угод зі spread 2-3%
✅ **Telegram повідомлення**: Сигнали, статус, помилки
✅ **Web Dashboard**: Моніторинг у реальному часі
✅ **Heartbeat система**: Перевірка роботи бота кожні 30с

## Архітектура

```
┌─────────────────┐
│  Blockchain RPC │
│  (ETH, BSC)     │
└────────┬────────┘
         │
         v
┌─────────────────┐      ┌──────────────┐
│  DEXScreener    │─────>│  Bot Logic   │
│  API            │      │  (bot.py)    │
└─────────────────┘      └──────┬───────┘
                                │
         ┌──────────────────────┼──────────────────────┐
         v                      v                      v
┌─────────────────┐    ┌──────────────┐      ┌──────────────┐
│  XT.com API     │    │  Telegram    │      │  Dashboard   │
│  (Trading)      │    │  (Notify)    │      │  (Web)       │
└─────────────────┘    └──────────────┘      └──────────────┘
```

## Швидкий старт

### 1. Вимоги

- Python 3.8+
- Встановлені залежності (вже зроблено)
- API ключі:
  - Telegram Bot Token
  - XT.com API Key & Secret

### 2. Конфігурація

Всі налаштування в `/app/backend/.env`:

```env
# Telegram
TELEGRAM_BOT_TOKEN=7603167137:AAFE7wYA8u1JcFwRFXsRl9zFL5eApqBSwOE
TELEGRAM_CHAT_ID=7820995179

# XT Exchange
XT_API_KEY=edbae47c-5dd1-4e17-85a5-4ddbf9a0198d
XT_API_SECRET=dc15cbd32da51249b35326dcc0bafb9045771fa8

# Trading Settings
ALLOW_LIVE_TRADING=True  # ⚠️ УВАГА: Реальна торгівля!
MIN_SPREAD_PERCENT=2.0
MAX_SPREAD_PERCENT=3.0
MIN_LIQUIDITY_USD=10000
MIN_VOLUME_24H_USD=50000
```

### 3. Запуск

#### Варіант A: Тільки бот

```bash
cd /app/backend
python3 main.py
```

#### Варіант B: Бот + Dashboard

```bash
cd /app/backend
bash run_bot_with_dashboard.sh
```

Дашборд буде доступний на: `http://localhost:8002`

#### Варіант C: Через скрипт

```bash
cd /app/backend
bash start_bot.sh
```

## Як це працює

### Процес сканування

1. **DEX Scanning** (кожні 10 секунд)
   - Отримує останні токени з DEXScreener
   - Фільтрує за ліквідністю та обсягом
   - Витягує ціни та метадані

2. **XT Lookup**
   - Шукає токен на XT.com
   - Отримує orderbook та ціну
   - Якщо не знайдено - скіп

3. **Spread Calculation**
   ```python
   spread = abs((dex_price - cex_price) / cex_price) * 100
   ```

4. **Signal Verification**
   - ✅ Spread: 2-3%
   - ✅ Ліквідність ≥ $10,000
   - ✅ Обсяг 24h ≥ $50,000
   - ✅ Токен на XT

5. **Execution** (якщо ALLOW_LIVE_TRADING=True)
   - Розраховує розмір позиції
   - Розміщує ордер на XT
   - Зберігає в trades.json
   - Відправляє повідомлення в Telegram

### Приклад сигналу в Telegram

```
🔔 Trading Signal

Token: PEPE (Pepe)
Chain: ethereum
DEX: uniswap-v2

Spread: 2.45%
DEX Price: $0.00001234
CEX Price: $0.00001204

Liquidity: $125,450
Volume 24h: $2,450,000

Action: EXECUTE
Reasons: All criteria met
```

## Файлова структура

```
backend/
├── main.py                    # Entry point
├── bot.py                     # Головна логіка бота
├── config.py                  # Конфігурація
├── utils.py                   # Допоміжні функції
├── blockchain_client.py       # Web3 для блокчейнів
├── dex_client.py             # DEXScreener API
├── xt_client.py              # XT.com API
├── signal_verification.py    # Верифікація сигналів
├── telegram_admin.py         # Telegram інтеграція
├── web_dashboard.py          # Dashboard (port 8002)
├── positions.json            # Активні позиції
├── trades.json               # Історія угод
└── bot_stats.json            # Статистика для dashboard
```

## API Endpoints

### Dashboard API

**GET /api/stats**
```json
{
  "signals_processed": 150,
  "valid_signals": 12,
  "trades_executed": 3
}
```

**GET /api/trades**
```json
[
  {
    "timestamp": "2025-10-27T14:00:00Z",
    "symbol": "PEPE_USDT",
    "side": "BUY",
    "quantity": 100,
    "order_id": "123456",
    "signal_data": {...}
  }
]
```

## Telegram команди

Бот автоматично відправляє:
- 🚀 Startup message
- 🔔 Trading signals
- ✅ Trade executions
- 💓 Heartbeat (кожні 30с)
- 🚨 Error alerts

## Критерії фільтрації

### Обов'язкові умови

| Параметр | Значення |
|----------|----------|
| Min Spread | 2.0% |
| Max Spread | 3.0% |
| Min Liquidity | $10,000 |
| Min Volume 24h | $50,000 |
| Token на XT | Обов'язково |

### Дії бота

- **SKIP**: Не відповідає критеріям
- **NOTIFY**: Spread > 3% (тільки сповіщення)
- **EXECUTE**: Всі критерії ОК + ALLOW_LIVE_TRADING=True

## Моніторинг

### Dashboard (Web)

1. Запустити: `bash run_bot_with_dashboard.sh`
2. Відкрити: http://localhost:8002
3. Статистика оновлюється кожні 10с

### Telegram

Heartbeat кожні 30 секунд:

```
💓 Bot Heartbeat

Uptime: 2h 15m
Signals processed: 450
Trades executed: 8
Active chains: ethereum, bsc
Last block: 23669240
```

### Логи

```bash
# Переглянути логи
tail -f /var/log/supervisor/backend.*.log

# Або якщо запущено вручну
# Логи виводяться в консоль
```

## Вирішення проблем

### Проблема: Мало сигналів

**Причини:**
- Більшість токенів не на XT
- Жорсткі критерії фільтрації
- Rate limits на DEXScreener

**Рішення:**
1. Зменшити мінімальну ліквідність:
   ```env
   MIN_LIQUIDITY_USD=5000
   ```

2. Розширити діапазон spread:
   ```env
   MIN_SPREAD_PERCENT=1.5
   MAX_SPREAD_PERCENT=4.0
   ```

### Проблема: RPC connection errors

**Причина:** Публічні RPC мають ліміти

**Рішення:**
1. Отримати платний RPC від Alchemy/Infura
2. Оновити .env:
   ```env
   ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY
   ```

### Проблема: Telegram не отримує повідомлення

**Перевірка:**
```bash
curl -X POST "https://api.telegram.org/bot<YOUR_TOKEN>/sendMessage" \
  -d "chat_id=<YOUR_CHAT_ID>&text=Test"
```

### Проблема: XT API errors

**Перевірка:**
```python
from xt_client import XTClient
xt = XTClient()
symbols = xt.get_symbols()
print(f"Found {len(symbols)} symbols")
```

## Безпека

### ⚠️ Важливо

1. **API ключі**
   - Ніколи не комітьте .env
   - Використовуйте права доступу 600
   ```bash
   chmod 600 /app/backend/.env
   ```

2. **Live Trading**
   - Спочатку тестуйте з ALLOW_LIVE_TRADING=False
   - Перевірте balance на XT
   - Почніть з малих сум

3. **Rate Limits**
   - DEXScreener: 1 req/sec
   - XT API: Див. документацію
   - Blockchain RPC: Залежить від провайдера

## Розширення

### Додати новий блокчейн

```python
# В blockchain_client.py
chain_configs = [
    ('ethereum', config.ETH_RPC_URL),
    ('bsc', config.BSC_RPC_URL),
    ('polygon', 'https://polygon-rpc.com'),  # Додати
]
```

### Змінити стратегію

```python
# В signal_verification.py
def verify_signal(self, dex_data, xt_data):
    # Додати власну логіку
    if custom_condition:
        result['valid'] = True
    return result
```

### Додати нову біржу

1. Створити `new_exchange_client.py`
2. Реалізувати методи: `get_ticker()`, `place_order()`
3. Додати в `bot.py`

## Performance

### Оптимізація

- **Async requests**: aiohttp для паралельних запитів
- **Caching**: TTL cache для token metadata
- **Rate limiting**: Уникнення блокувань
- **Reconnect logic**: Автоматичне відновлення з'єднань

### Метрики

- **Latency**: RPC < 300ms, DEX API < 2s
- **CPU**: < 70% на 2 vCPU
- **RAM**: < 70% на 2 GB
- **Scan rate**: ~30 токенів / 10 секунд

## Підтримка

### Корисні команди

```bash
# Перевірити статус
ps aux | grep python

# Вбити процес
pkill -f "python3 main.py"

# Очистити логи
> /var/log/supervisor/backend.err.log

# Перезапустити
sudo supervisorctl restart backend
```

### Debug режим

```python
# В utils.py змінити рівень логування
logging.basicConfig(level=logging.DEBUG)
```

## Ліцензія

МІТ

## Контакти

Питання? Проблеми? 
- Telegram: @your_channel
- GitHub Issues

---

**Створено з ❤️ для крипто-трейдерів**