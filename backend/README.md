# Trinkenbot Enhanced

Автоматизований торговий бот для DEX арбітражу з інтеграцією біржі XT.com

## Функціональність

- 🔍 **Моніторинг блокчейнів**: Ethereum, BSC, Solana
- 📈 **DEX Сканер**: Інтеграція з DexScreener для пошуку торгових пар
- 💱 **Інтеграція XT.com**: Торгівля на централізованій біржі
- 🤖 **Автоматична торгівля**: Виконання угод зі spread 2-3%
- 📢 **Telegram повідомлення**: Сигнали та статус бота
- ✅ **Верифікація сигналів**: Фільтрація по ліквідності, обсягу та spread

## Вимоги

- Python 3.8+
- MongoDB (для FastAPI сервера)
- API ключі:
  - Telegram Bot Token
  - XT.com API Key & Secret

## Налаштування

### 1. Встановлення залежностей

```bash
cd /app/backend
pip install -r requirements.txt
```

### 2. Конфігурація .env

Всі необхідні змінні вже налаштовані в `/app/backend/.env`:

```env
# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id

# XT Exchange
XT_API_KEY=your_api_key
XT_API_SECRET=your_api_secret

# Trading Settings
ALLOW_LIVE_TRADING=True
MIN_SPREAD_PERCENT=2.0
MAX_SPREAD_PERCENT=3.0
MIN_LIQUIDITY_USD=10000
MIN_VOLUME_24H_USD=50000
```

### 3. Запуск бота

```bash
# Через Python
cd /app/backend
python3 main.py

# Або через скрипт
bash start_bot.sh
```

## Архітектура

```
backend/
├── main.py                    # Точка входу
├── bot.py                     # Основна логіка бота
├── config.py                  # Конфігурація
├── utils.py                   # Утиліти
├── blockchain_client.py       # Web3 клієнт для блокчейнів
├── dex_client.py             # DexScreener API
├── xt_client.py              # XT.com API
├── signal_verification.py    # Верифікація торгових сигналів
├── telegram_admin.py         # Telegram інтеграція
├── positions.json            # Збережені позиції
└── trades.json              # Історія угод
```

## Як це працює

1. **Сканування DEX**: Бот періодично отримує останні торгові пари з DexScreener
2. **Пошук на XT**: Для кожної пари шукає відповідний символ на біржі XT
3. **Розрахунок spread**: Обчислює різницю цін між DEX та CEX
4. **Верифікація**: Перевіряє критерії (spread 2-3%, мін. ліквідність, обсяг)
5. **Виконання**: При проходженні всіх перевірок виконує угоду на XT
6. **Повідомлення**: Надсилає статус в Telegram

## Критерії фільтрації

- **Spread**: 2% - 3%
- **Мінімальна ліквідність**: $10,000
- **Мінімальний обсяг 24h**: $50,000
- **Наявність на XT**: Токен має бути доступний для торгівлі

## Heartbeat

Бот відправляє статус кожні 30 секунд:
- Час роботи
- Кількість оброблених сигналів
- Виконані угоди
- Активні блокчейни
- Останній блок

## Безпека

- API ключі зберігаються в .env
- Підтримка retry механізму для API викликів
- Автоматичний reconnect до RPC
- Логування всіх операцій

## Вирішення проблем

### Проблема: Мало сигналів після угод

**Рішення**: 
- Збільшено rate limiting між запитами до DEX
- Додано reconnect логіку для blockchain клієнта
- Оптимізовано обробку пар (батчінг)
- Додано кешування для уникнення дублікатів

### Логи

Всі логи виводяться в консоль з інформацією:
- INFO: Нормальна робота
- WARNING: Попередження (не знайдено токен, низький spread)
- ERROR: Помилки (API timeout, невдала угода)

## Моніторинг

Перевірка статусу через Telegram:
- Отримуйте heartbeat кожні 30 секунд
- Сигнали з деталями про можливості
- Підтвердження виконаних угод
- Сповіщення про помилки

## Підтримка

Для питань та підтримки:
- Перевірте логи: `tail -f /var/log/supervisor/backend.*.log`
- Перевірте статус: Telegram heartbeat повідомлення
- Перегляньте trades.json для історії угод